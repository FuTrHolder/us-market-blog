# ── AI 모델 (완전 무료) ───────────────────────
google-genai>=1.0.0      # Gemini 2.0 Flash — 무료 API (1,500 req/day, 신용카드 불필요)

# ── 시장 데이터 (완전 무료) ──────────────────
yfinance>=0.2.40         # 지수, 섹터, 선물, 실적 캘린더
pandas>=2.0.0            # 데이터 처리 + FRED CSV 파싱

# ── 썸네일 생성 (완전 무료) ──────────────────
matplotlib>=3.8.0        # 차트 렌더링
Pillow>=10.0.0           # 이미지 처리
numpy>=1.26.0            # 수치 연산

# ── Google Blogger 인증 (완전 무료) ──────────
PyJWT>=2.8.0             # 서비스 계정 JWT 인증
cryptography>=42.0.0     # RSA 서명

# ── 유틸리티 ─────────────────────────────────
requests>=2.31.0
python-dotenv>=1.0.0     # 로컬 .env 파일 지원
